[defaults]
# Opt into the future default — suppresses INJECT_FACTS_AS_VARS deprecation warning
inject_facts_as_vars = false

# Run against localhost only
inventory           = inventory

# Cleaner output (community.general yaml callback removed in v12; use built-in default with yaml format)
stdout_callback     = default
result_format       = yaml
