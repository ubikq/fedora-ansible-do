---
# =============================================================================
# vars/niri.yml — Niri Compositor + DankMaterialShell Configuration
# =============================================================================
# Values used when rendering templates/niri-config.kdl.j2 on first deploy.
# Re-running the niri tag will NOT overwrite an existing config.kdl — edit
# this file and delete ~/.config/niri/config.kdl to force a fresh deploy.
# =============================================================================

# ── Startup programs ──────────────────────────────────────────────────────────
niri_polkit_agent: /usr/lib/polkit-gnome-authentication-agent-1

# Flatpak app ID used in the syncthingtray startup command
niri_syncthingtray_flatpak: io.github.martchus.syncthingtray
